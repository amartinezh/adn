export class TarifasSobrepaso {
    diferencia: string;
    valor: number;

    constructor(diferencia: string, valor: number) {
        this.diferencia = diferencia;
        this.valor = valor;
    }
}
